@{
    ViewData["Title"] = "UploadFiles";
}

<img src="~/images/upload.jpg" width="200px" height="150px"/>

<hr />
<div class="jumbotron jumbotron-fluid">
    <div class="container">
        @if (ViewData["Erro"] != null)
        {
            <img src="~/images/erro.png" width="150" height="150"/>
            <h4>@ViewData["Erro"]"</h4>
        }
    </div>

    <div class="container">
        @if (ViewData["Resultado"] != null)
        {
            <h4>@ViewData["Resultado"]</h4>
            <hr />
            @if(ViewBag.Arquivos != null)
            {
                @foreach(string arquivo in ViewBag.Arquivos)
                {
                    <li>
                        @arquivo
                    </li>
                }
            }
        }
    </div>
</div>
<br />
<div>
    <a asp-area="Admin" asp-controller="AdminImagens" asp-action="Index" class="btn btn-secondary"><h5>🔙 Voltar</h5></a>
</div>